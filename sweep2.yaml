command:
  uv run example.py
  --batch-size ${batch_size}
experiment_name: sweep-demo
sweep_name: test-sweep
method: grid
metric:
  name: metric1
  goal: maximize
parameters:
  batch_size:
    values: [16, 32, 64, 128]
run_cap: 10
